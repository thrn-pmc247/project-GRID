"""Database layer: SQLAlchemy models, session factories, Alembic migrations.

Schema contract with field-level PDPA classes: docs/context/data-model.md — design
there before writing a migration. Personal data lives only in the practitioner
table (restricted).
"""
