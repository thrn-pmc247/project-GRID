"""Exports — xlsx.py builds the PNM-facing branded workbook (openpyxl).

Brand tokens: docs/context/brand.md (Arial; navy header rows with white bold text).
Workbooks are written to data/exports/ (gitignored) — never to the repo root
(check 13 rejects stray *.xlsx). Phase 1.
"""
