# Claude Design handoff brief — PNM dashboard

> **Placeholder.** This brief is a Phase 3 deliverable, produced once the API is
> real. Scope commitments (from the project brief): user stories, the exact fields
> the dashboard shows, filter/sort dimensions, the KPI widget spec (40–60/month
> against `kpi_month`), empty/loading/error states, and the PMCare brand tokens
> (`docs/context/brand.md`). The dashboard UI itself is built by Claude Design, not
> in this repo — this repo ships only `openapi.json` and this brief.
